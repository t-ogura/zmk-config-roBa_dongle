on: [push, pull_request, workflow_dispatch]
jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      config_path: config
      build_matrix: |
        board: seeeduino_xiao_ble
        shield: roBa_dongle
        artifact_name: roBa_dongle
        cmake_args: -DSHIELD=roBa_dongle
